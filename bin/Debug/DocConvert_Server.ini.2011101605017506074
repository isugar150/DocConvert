[DC Server]
LicenseKEY=;정품 확인키
BindIP=Any;바인드할 IP주소 (Any: 아무 주소)
WebSocketPort=12000;WebSocket Listning할 포트
FileServerPort=12100;감시할 파일전송 포트
DisplayLogCnt=300;리스트박스에 표시할 최대 로그카운트 (단위: 개)
ClientKEY=B29D00A3 - F825 - 4EB7 - 93C1 - A77F5E31A7C2;해당 클라이언트가 허용한 클라이언트인지 확인하는 문자열, 클라이언트와 동일하게 맞추면됨.
DataPath=C:\Data;작업폴더 실제 경로
OfficeDebugModeYn=Y;오피스 변환시 변환하는 화면보이기 (Y:사용) (n:사용안함)
ShowTextBoxYn=Y;프로그램 시작시 TextBox 체크 (Y:사용) (n:사용안함)
FollowTailYn=n;프로그램 시작시 FollowTail 체크 (Y:사용) (n:사용안함)
ResponseTimeout=2,0;서버에서 클라이언트로 응답 Timeout 서버 환경에따라 설정 (분,초)
SchedulerTime=1,0;스케줄러 동작시간(매일) 24시간중 (시간,분)
CleanWorkspaceSchedulerYn=Y;작업공간 정리 스케줄러 사용 (Y:사용) (n:사용안함)
CleanWorkspaceDay=3;작업공간 정리시 설정한 오래된 일수가 지난 파일 삭제 (단위: 일)
CleanLogSchedulerYn=Y;로그 정리 스케줄러 사용 (Y:사용) (n:사용안함)
CleanLogDay=10;로그 정리시 설정한 오래된 일수가 지난 파일 삭제 (단위: 일)
ChromiumCaptureYn=n;(Y:크로미움기반 웹 문서 캡쳐 사용) (n:Phantom JS를 이용한 웹 문서 캡쳐)
WebCaptureTimeout=30;특정 시간이지나면 캡쳐중인 콘솔창 자동 Kill (단위: 초)

[DRM Setting]
DRM useYn=;DRM 사용 여부
DRM Path=;DRM 경로
DRM Result=0;DRM 성공 시 Result 코드 (해당 코드가 아니면 실패처리)
DRM Args[0]=$Full_Path$,123 ;DRM 아규먼트 ','로 구분 { 풀 경로($Full_Path$), 파일 경로($File_Path$), 파일 명($File_Name$ }

